/**
 * Yield already harvested and donated to impact programs before the on-chain
 * vault counter reflects it. Update after each off-chain disbursement.
 */
export const PRIOR_YIELD_DONATED_USD = 0;
